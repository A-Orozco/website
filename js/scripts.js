$(document).ready(function() {
    alert('your link is red now');
    // alt selector = jQuery('a')...
    $('a').css('color','red');
    var user = $('#userName').val();
    var pass = $('#pass').val();

    var creds = {
        userName: 'user',
        passWord: 'pass'
    }

    
});